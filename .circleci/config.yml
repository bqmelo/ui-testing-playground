version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          command: npm run cy:run
          post-steps:
            - store_test_results:
                path: cypress/reports/mocha-reports/mochawesome.json
            - store_artifacts:
                path: report/